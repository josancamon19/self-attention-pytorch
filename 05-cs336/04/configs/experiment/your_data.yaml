# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: tokenized_data.bin
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin: tokenized_paloma_c4_100_domains_validation.bin
  
  model_output: output/your_data

training:
  wandb_entity: josancamon19-cifrato  # Set to your wandb entity, e.g. pliang
  wandb_project: assignment-04